# @dpopsuev/alef-agent-runtime

Alef backend runtime for sessions, tools, resource loading, and RPC transports.

This package provides the reusable runtime layer that sits between
`@dpopsuev/alef-agent-core` and the Alef CLI/TUI app.
