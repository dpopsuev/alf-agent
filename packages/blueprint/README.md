# @dpopsuev/alef-agent-blueprint

Declarative agent blueprints for Alef.

This package owns YAML agent-definition types, compilation/loading helpers,
organ-to-tool mapping metadata, and shipped default bootstrap blueprints such as
`GenSec` and `2Sec`.
